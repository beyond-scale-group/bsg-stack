<!-- fingerprint: 2d96726cf88badb1 -->
# Tech Health — 2026-07-03

**Repository:** `bsg-stack`
**Generated:** 2026-07-03T03:32:12Z
**Ecosystems:** 

---

## Dependency Health

**Summary:** 0 tracked — 0 up-to-date, 0 patch-behind, 0 minor-behind, 0 major-behind

_No major-version-behind dependencies._

---

## Code Quality

**Summary:** 32 source files analyzed — 3 oversized (> 500 LOC), 5 function-dense (> 20), 0 circular-import cycles

### Largest files

| File | Lines | Functions |
|------|-------|-----------|
| claude-skills/skills/md-to-office/scripts/scan-brand.py | 886 | 29 |
| claude-skills/tests/test_po_report_integration.py | 568 | 18 |
| claude-skills/scripts/update-bsg-skills.py | 523 | 16 |



---

## Tech Debt Inventory

**Summary:** 0 markers — TODO: 0, FIXME: 0, HACK: 0; oldest: n/a

**Debt score:** 0 (no previous)

_No stale TODOs (> 90 days)._

---

## Architecture Decision Records

**ADR directory:** `.bsg/adr` (3 entry/entries)

- 001 — ADR-001: Unified `.bsg/` directory convention (unknown)
- 002 — ADR-002: `/bsg-stack doctor` skill contract (unknown)
- 003 — ADR-003: `DESIGN.md` schema — adopt Google Stitch spec (unknown)

## Phase (A.5) — Audit-to-issue

Same 3 oversized-file findings as the prior sweep, each already carrying
an open self-filed issue (#690, #691, #692). No new findings without an
existing issue this tick.

---

## Phase (B) — Implementation pilot

Circuit-breaker: clear. Walked `list-pilot-candidates.sh --agent tech`
(15 open `needs:tech` candidates) oldest-first by issue number:

- **#617** — "chore: migrate .bsg/ paths" — skipped a **second**
  consecutive tick (matches `never-auto-implements: "refactors without
  a bug to fix"`). Per policy, `needs:tech` removed and a disposition
  comment posted (marker `bsg-pilot-ineligible:tech`) — handed back to
  the PO/human.
- **#618, #619, #621** — "implement --init action" for po/storytelling/
  security — all three already shipped in #594 and #614
  (`init-plan.sh`, `init-narrative.sh`, `init-securityignore.sh` exist
  and are wired into `bsg-stack init`'s orchestrator). No delta to
  implement; skipped.
- **#636** — "weekly-plan.sh + Google Calendar" — multi-component
  feature requiring UX/heuristic design decisions (day-assignment
  priority rules, calendar event schema) a mechanical pilot shouldn't
  decide (principle #5). Skipped.
- **#645** — "md-to-word TOC missing in LibreOffice PDF export" —
  fix is exploratory ("piste de fix", unconfirmed approach) and
  `soffice` is not available in this environment, so no way to write
  a real failing test. Skipped.
- **#657** — "reconcile-milestones.sh silently assigns 0 milestones"
  (bug, root cause + suggested fix in the issue body) — **implemented**.
  Added a regression test reproducing the jq `as $var`-on-non-matching-
  `capture()` short-circuit (confirmed failing pre-fix), then read the
  explicit slug from `.bindings.milestones[0]` and collapsed the
  inferred-from-title `capture()` to a single-output `[expr][0]` before
  binding. Test passes post-fix; existing #546/#288 regression suites
  still pass. PR #721 opened and auto-merged (`auto_merge: true`),
  issue #657 closed, `human-reviewed` stamped by the helper.

## Phase (C) — Peer review

`peer-review-candidates.sh --reviewer tech`: no eligible PRs this tick.

---

pilot: attempted #657 — PR #721
