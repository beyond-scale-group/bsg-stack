# Dev planning order — beyond-scale-group/bsg-stack

_Generated: 2026-04-30 from PLAN.md + open backlog snapshot_

11 open issues across 4 epics + 4 unbound. Recommended order below
prioritises in-flight work first, then foundations that unblock downstream
items, then catalog-growth, then unbound items that need triage.

## Wave 1 — close out in-flight (this week)

Items already shipped or with a PR awaiting review.

| # | Title | Why now |
|---|---|---|
| **PR #249** | fix: skip broken paths in updater (closes #67) | Already opened by tech-lead pilot; merge unblocks E2 and proves the autopilot pipeline end-to-end. |
| **#200** | Per-agent scope contract in frontmatter | **Likely already shipped** — CLAUDE.md documents `auto-implements` / `never-auto-implements` as live and `test_skills.py` enforces them. Verify and close. |
| **#222** | Full autonomy mode | **Likely already shipped** — autopilot mode (`.bsg-autopilot.yml`), audit-to-issue, peer review are all live in CLAUDE.md. Verify and close. |

## Wave 2 — foundations that unblock everything else

Bug + plan-bound items where shipping them removes friction from many
other agents and skills.

| # | Title | Epic | Rationale |
|---|---|---|---|
| **#199** | Bus primitives are decorative — agents don't call `bus_claim` / `bus_lock` / `bus_handoff` | (scope-creep, needs epic) | Foundational bug: the coordination bus exists in scripts but no agent actually uses it. Without this, autopilot delegation between agents is non-functional. Bind to a new **E7-bus-activation** epic. |
| **#128** | parse-plan.sh silent failure + plan validator | E6-plan-schema-hygiene | Plan listed as in-flight but no PR yet — quick bash fix + a unit test. |

## Wave 3 — plan-bound enhancements (next sprint)

| # | Title | Epic | Effort |
|---|---|---|---|
| **#104** | `/learn` short-circuit on unchanged input | E5-learn-skill-dedup | Small — one fingerprint check, mirrors what security/seo already do. |
| **#115** | `bootstrap-plan.sh` seeds epics from open issues when no milestones | E4-catalog-growth | Small — pure jq transform of the existing snapshot. |
| **#62** | Per-domain intent files (ROADMAP.md, ARCHITECTURE.md, BRAND.md) as agent context | E4-catalog-growth | Medium — convention + each agent reads its file. |
| **#63** | `@cleaner` agent — prune obsolete issues and reorder backlogs | E4-catalog-growth | Medium — new agent in the registry. Will partially overlap with what this very report does. |

## Wave 4 — unbound, need triage before scheduling

| # | Title | Recommendation |
|---|---|---|
| **#237** | mandatory `--init` on all agents/skills + `/doctor` skill for repo bootstrap | Bind to **new epic E8-bootstrap-ergonomics**. Two-phase: split `--init` enforcement (small, CI test) from `/doctor` skill (larger). |
| **#241** | `init-brand.sh` overwrites manual `tokens.json` edits + misses Tailwind v4 `@theme` tokens | Bug on the freshly-shipped md-to-office skill. Bind to **E8** alongside #237. Small fix. |
| **#250** | md-to-office: PPTX target + tool-agnostic format frontmatter | Enhancement on md-to-office. Bind to a new **E9-office-formats** or fold into the md-to-office PRD. Medium effort. |

## Suggested execution order

```
1. Merge PR #249                         (tech-lead pilot)
2. Verify and close #200, #222           (po-manager — paperwork)
3. #199                                  (tech — foundational bus wiring)
4. #128                                  (tech — plan validator)
5. #104                                  (tech — quick token win)
6. #115                                  (po — bootstrap UX)
7. #241                                  (tech — md-to-office bug)
8. #62 + #63                             (tech — catalog growth, parallel)
9. Split #237 into two issues, then ship --init enforcement first
10. #250                                 (after #237 lands)
```

## Plan structure changes recommended

1. **Add E7-bus-activation** for #199 — the bus is foundational and currently
   labeled scope-creep, which is wrong.
2. **Add E8-bootstrap-ergonomics** for #237 + #241 — `--init` + `/doctor` are
   a coherent UX theme.
3. **Add E9-office-formats** (or rename E4) to absorb md-to-office follow-ups
   like #250.
4. **Mark E2-install-reliability done** once PR #249 merges (its only ticket).

## Drift summary (from PLAN.md)

- 4 issues currently labeled `scope-creep`: #199, #200, #222, #237, #241.
  Of those, #200 and #222 should close, #199/#237/#241 just need new epics.
  None are genuine non-goal violations.
