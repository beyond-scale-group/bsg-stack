# Cleaner hygiene report — 2026-05-04

## Summary

- **Stale `agent:lock:*` labels found:** 0
- **Near-duplicate issue pairs detected:** 0
- **Manifest GC findings:** 0
- **Label inconsistency fixed:** 2 agent-filed issues missing `needs-human-review` — label added to #330 and #331

## Lock cleanup

No `agent:lock:*` labels found on any open issue. Nothing to do.

## Duplicate detection

All 7 open issue titles checked for Levenshtein distance <= 10% of the longer title.
No candidate pairs found.

## `needs:*` / `agent:done` cleanup

No closed issues carry stale `needs:*` or `agent:done` labels. Clean.

No open issues carry `agent:done`. Clean.

## Manifest GC

No open issues carrying an `agents-state` manifest block with `phase == "done"` found.

## Label inconsistency — agent-filed issues missing `needs-human-review`

Per the CLAUDE.md convention, every agent-filed issue must carry `needs-human-review`.
Two open issues filed by the tech agent were missing this label:

| Issue | Title | Action |
|---|---|---|
| #331 | Split oversized file claude-skills/tests/test_skills.py (599 LOC) | Added `needs-human-review` |
| #330 | Split oversized file claude-skills/tests/test_md_to_office.py (1000 LOC) | Added `needs-human-review` |

Both labels have been applied.

## Unused label report

The following labels have 0 open issues (report only — no labels deleted):

```
documentation
duplicate
good first issue
help wanted
invalid
question
wontfix
needs-human-review
qa
security
seo
marketing
pr-comms
storytelling
scope-creep
peer-reviewed:qa
peer-reviewed:tech
needs-rework
peer-reviewed:security
filed-by:qa
peer-reviewed:seo
filed-by:seo
peer-reviewed:po
epic:E10-label-simplification
needs:tech
```

Note: `needs-human-review`, bus labels (`qa`, `security`, `seo`, `marketing`, `pr-comms`, `storytelling`),
`peer-reviewed:*`, and `filed-by:*` are infrastructure labels — zero open-issue count is expected
when no active work is in flight.

No `meta:backlog` issue exists in this repo — unused label report is logged here instead.

## Silence-breakers fired

- **Manifest / label inconsistency:** YES — 2 agent-filed issues (#330, #331) lacked `needs-human-review`; label applied to both.
