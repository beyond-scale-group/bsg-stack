# Cleaner hygiene report — 2026-05-04

## Summary

- **Stale `needs:*` labels removed from closed issues:** 2
- **Stale `agent:lock:*` labels found:** 0
- **Near-duplicate issue pairs detected:** 0
- **Manifest GC findings:** 0

## Lock cleanup

No `agent:lock:*` labels found on any open issue. Nothing to do.

## Duplicate detection

All 8 open issue titles checked for Levenshtein distance <= 10% of the longer title.
No candidate pairs found.

## `needs:*` / `agent:done` cleanup

Two closed issues were carrying a stale `needs:tech` routing label:

| Issue | Title | Action |
|---|---|---|
| #313 (CLOSED) | bug(installer): update-bsg-skills.py reports "updated" without refreshing existing files | Removed `needs:tech` |
| #288 (CLOSED) | feat(po): po-manager reads milestones instead of epic:* labels | Removed `needs:tech` |

Both labels were removed. Open issues carry no stale `needs:*` routing at this time.

## `agent:done` on re-opened issues

No open issues carry `agent:done`. Clean.

## Manifest GC

No open issues carrying an `agents-state` manifest block with `phase == "done"` found.

## Unused label report

The following labels have 0 open issues (report only — no labels deleted):

```
documentation
duplicate
good first issue
help wanted
invalid
question
wontfix
needs-human-review
qa
security
seo
marketing
pr-comms
storytelling
scope-creep
peer-reviewed:qa
peer-reviewed:tech
needs-rework
peer-reviewed:security
filed-by:qa
peer-reviewed:seo
filed-by:seo
peer-reviewed:po
epic:E10-label-simplification
needs:tech
```

Note: `needs-human-review`, bus labels (`qa`, `security`, `seo`, `marketing`, `pr-comms`, `storytelling`),
`peer-reviewed:*`, and `filed-by:*` are infrastructure labels — zero open-issue count is expected
when no active work is in flight. The `needs:tech` label was just cleaned from the two closed issues.

No `meta:backlog` issue exists in this repo — unused label report is logged here instead.

## Silence-breakers fired

- `needs:*` stale on closed issues: **YES** (2 issues cleaned)
