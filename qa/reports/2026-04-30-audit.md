<!-- fingerprint: none -->
# QA Audit — 2026-04-30

**Repository:** `bsg-stack`
**Generated:** 2026-04-30T13:58:23Z
**Test suite present:** true

---

## Coverage Summary

_No coverage report found. CI does not emit lcov.info, coverage-summary.json, coverage.xml, .coverage, or coverage.out. Configure the test workflow to emit a coverage artifact for delta tracking._

---

## Regression Risk Heatmap

**Summary:** 0 files analyzed — 0 critical, 0 high, 0 moderate

_No high-risk files detected (no coverage data available to cross with churn)._

---

## Flaky Tests (last 14 days)

**Summary:** 50 CI runs analyzed, 0 flake(s) detected (0 new)

_No flaky tests detected in the window._

---

## CI Failures — Silence-Breaker

**2 consecutive CI failures on `main`** — `claude-skills test` workflow fails at commit `13223fc` (PR #294) and its predecessor `dfcfe50`.

**Root cause:** `claude-skills/tests/test_pilot_candidates.sh` was written against the old per-issue gate behavior (asserting `--label safe-to-automate` and `--label human-reviewed` appear in `gh issue list` args). PR #294 rewrote `list-pilot-candidates.sh` to exit 0 immediately when no `.bsg-autopilot.yml` is present — `gh issue list` is never called in that path, so the `gh_args` capture file is never written. Test 1 fails with:

```
cat: /tmp/.../gh_args: No such file or directory
```

**Affected tests:**
- Test 1 (no autopilot file): asserts gates are present — now irrelevant; script exits before any `gh` call
- Test 3 (agent not listed): same root cause
- Test 4 (autopilot disabled): same root cause

**Required fix:** Update Tests 1, 3, 4 to assert that the script emits empty output (no candidates) rather than inspecting `gh_args` for label flags that no longer exist. The `safe-to-automate` / `human-reviewed` label path is gone from `list-pilot-candidates.sh` as of #294.

---

## Untested Areas

No notable untested areas flagged (no coverage data available).

---

## Pilot

pilot: no candidates

---

_Generated by `@qa tick` · 2026-04-30_
